import { RoutePlaceholder } from "@/app/_lib/route-placeholder";

export default function Page() {
  return (
    <RoutePlaceholder
      section="Public"
      route="/"
      title="Nalarin is almost ready"
      description="Beranda baru sedang kami siapkan dengan fondasi App Router yang lebih rapi, modern, dan siap dikembangkan per section."
    />
  );
}
